th:attr="data-pk=${row.primaryKeyJson}"


return list.stream()
        .filter(r -> r.getPrimaryKey().equals(pk))
        .findFirst()
        .orElse(null);

th:attr="data-pk=${#objectMapper.writeValueAsString(row.primaryKey)}"
------

public String getPrimaryKeyJson() throws Exception {
    return new ObjectMapper().writeValueAsString(primaryKey);
}

-------
const pk = btn.dataset.pk;
const pk = encodeURIComponent(btn.dataset.pk);



------------

@GetMapping("/row")
@ResponseBody
public RowDiffDTO getRow(
        @RequestParam String cacheId,
        @RequestParam String pk,
        @RequestParam String tab) throws Exception {

    Map<String,Object> primaryKey =
        new ObjectMapper().readValue(pk, Map.class);

    return service.getRowDetails(cacheId, primaryKey, tab);
}


-------

if (!res.ok) {
    const text = await res.text();
    console.error("Server error:", text);
    return;
}

const data = await res.json();


